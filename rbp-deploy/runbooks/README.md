# Runbooks

Operational runbooks for bootstrap, deploy failure handling, rollback, and common failure domains.

Each runbook is a starter template and should be updated with real command snippets and owner contacts in Step 3.

Canonical branch model for this repo: PR -> `master` -> dev -> staging -> prod.
