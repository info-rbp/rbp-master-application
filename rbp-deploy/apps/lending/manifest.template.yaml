# Template deployment manifest for lending
apiVersion: deploy.rbp/v1
kind: ServiceDeployment
metadata:
  name: lending
spec:
  enabled: true
  runtime: lending_engine
  replicas:
    dev: 1
    staging: 1
    prod: <replace-me>
  image:
    repository: "<replace-me>/lending"
    tag_strategy: "<git-sha|semver>"
  resources:
    cpu: "<replace-me>"
    memory: "<replace-me>"
  health:
    liveness_path: "<replace-me>"
    readiness_path: "<replace-me>"
