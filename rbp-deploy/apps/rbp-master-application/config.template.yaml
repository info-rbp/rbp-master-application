# Non-secret runtime config template for rbp-master-application
service: rbp-master-application
config:
  public_base_url: "<replace-me-if-applicable>"
  internal_base_url: "<replace-me-if-applicable>"
  log_level: "info"
  feature_profile: "default"
  timeout_ms: 8000
