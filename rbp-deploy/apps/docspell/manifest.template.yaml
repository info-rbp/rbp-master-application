# Template deployment manifest for docspell
apiVersion: deploy.rbp/v1
kind: ServiceDeployment
metadata:
  name: docspell
spec:
  enabled: true
  runtime: document_ingest_engine
  replicas:
    dev: 1
    staging: 1
    prod: <replace-me>
  image:
    repository: "<replace-me>/docspell"
    tag_strategy: "<git-sha|semver>"
  resources:
    cpu: "<replace-me>"
    memory: "<replace-me>"
  health:
    liveness_path: "<replace-me>"
    readiness_path: "<replace-me>"
