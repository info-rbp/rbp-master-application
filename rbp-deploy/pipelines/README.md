# Pipeline templates

This folder contains starter pipeline definitions for validation, deployment, promotion, and rollback.

These are provider-agnostic templates and should be adapted to GitHub Actions / GitLab CI / Cloud Build in Step 3.
