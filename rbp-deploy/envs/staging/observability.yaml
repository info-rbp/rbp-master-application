# Template: observability sinks for staging
environment: staging
logging:
  provider: "<replace-me>"
  destination: "<replace-me>"
metrics:
  provider: "<replace-me>"
  dashboard_namespace: "rbp/staging"
alerting:
  on_call_schedule: "<replace-me>"
  channels:
    - "<pager|slack|email>"
tracing:
  correlation_id_header: "x-correlation-id"
