# Template: observability sinks for dev
environment: dev
logging:
  provider: "<replace-me>"
  destination: "<replace-me>"
metrics:
  provider: "<replace-me>"
  dashboard_namespace: "rbp/dev"
alerting:
  on_call_schedule: "<replace-me>"
  channels:
    - "<pager|slack|email>"
tracing:
  correlation_id_header: "x-correlation-id"
